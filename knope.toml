[package]
versioned_files = [
  "Cargo.toml",
  "Cargo.lock",
  "bindings/node/Cargo.toml",
  "bindings/wasm/Cargo.toml",
  "bindings/python/Cargo.toml",
  "bindings/browser/Cargo.toml",
  "bindings/node/package.json",
  "bindings/node/npm/linux-x64-gnu/package.json",
  "bindings/node/npm/win32-x64-msvc/package.json",
  "bindings/node/npm/linux-arm64-gnu/package.json",
  "bindings/node/npm/darwin-x64/package.json",
  "bindings/node/npm/darwin-arm64/package.json",
  "npm/turndb/package.json",
  "cli/package.json",
  "cli/npm/linux-x64-gnu/package.json",
  "cli/npm/linux-arm64-gnu/package.json",
  "cli/npm/darwin-x64/package.json",
  "cli/npm/darwin-arm64/package.json",
  "cli/npm/win32-x64-msvc/package.json",
]
changelog = "CHANGELOG.md"

[github]
owner = "turndb"
repo = "turndb"

[changes]
# Release intent lives in reviewed change files. Conventional-looking commit subjects must not
# silently enlarge or reclassify the version prepared by those files.
ignore_conventional_commits = true

[[workflows]]
name = "document-change"

[[workflows.steps]]
type = "CreateChangeFile"

[[workflows]]
name = "prepare-release"

[[workflows.steps]]
type = "Command"
command = "git switch -C release"

[[workflows.steps]]
type = "PrepareRelease"

[[workflows.steps]]
type = "Command"
command = "python3 scripts/sync-release-version.py $version"

[[workflows.steps]]
type = "Command"
command = "node bindings/browser/build-viewer.mjs"

[[workflows.steps]]
type = "Command"
command = "python3 scripts/check-release-metadata.py"

[[workflows.steps]]
type = "Command"
command = "git add --all"

[[workflows.steps]]
type = "Command"
command = "git commit -m \"chore: prepare release $version\""

[[workflows.steps]]
type = "Command"
command = "git push --force --set-upstream origin release"

[[workflows.steps]]
type = "CreatePullRequest"
base = "main"

[workflows.steps.title]
template = "chore: prepare release $version"

[workflows.steps.body]
template = "This PR was created by Knope. Review the generated CHANGELOG.md entry and all lockstep version changes. Merging it creates the v$version release after the protected publication jobs are approved.\n\nThe native platform packages remain versioned unresolved optional entries in `bindings/node/package-lock.json`. That committed shape is valid before and after first publication; there is no post-publication lock repair."

# There is deliberately no `release` workflow here. Knope's Release step treats an existing tag at
# the prepared version as evidence the release is already done, and this repository creates the
# annotated tag before releasing so the publication workflows have an annotated object to verify.
# The two cannot both be satisfied, so tagging and the GitHub release are driven directly by
# `.github/workflows/release.yml`. Re-adding a Release step here would restore a silent no-op.
